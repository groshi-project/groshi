database objects:

users: // there should be default user root
    fields:
        login, password
    actions:
        create
        delete
        edit
        get

transactions:
    fields:
        amount, currency, description, date, file attachments
    actions:
        create
        delete
        edit
        get (with filters and pagination)
